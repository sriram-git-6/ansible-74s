- name: variables from inventory
  hosts: mongodb
  tasks:
    - name: print the variables from inventory
      ansible.builtin.debug:
        msg: "mongodb user is {{username}} and password is {{password}}"